[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "forgetools-mcp-linux"
version = "0.1.0"
description = "Domain MCP server for linux host operations"
readme = "../../README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.scripts]
forge-mcp-linux = "forgetools.mcp_linux_server:main"

[tool.hatch.build.targets.wheel]
only-include = ["pyproject.toml"]
