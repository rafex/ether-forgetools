[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "forgetools-mcp-java"
version = "0.1.0"
description = "Domain MCP server for java quality and standards"
readme = "../../README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.scripts]
forge-mcp-java = "forgetools.mcp_java_server:main"
