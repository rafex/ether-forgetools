[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "forgetools-mcp-websearch"
version = "0.1.0"
description = "Domain MCP server for web search and navigation"
readme = "../../README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.scripts]
forge-mcp-websearch = "forgetools.mcp_websearch_server:main"
