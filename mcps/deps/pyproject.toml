[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "forgetools-mcp-deps"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["forgetools"]

[project.scripts]
forge-mcp-deps = "forgetools.mcp_deps_server:main"

[tool.hatch.build.targets.wheel]
only-include = ["pyproject.toml"]
